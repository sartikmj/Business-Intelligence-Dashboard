import express from 'express'

const router = express.Router(); //It creates a mini Express application (called a router) that you can use to define routes separately and then attach to the main app.

export default router; //It exports the router object as the default export of the file.